/**
 * Part 4: Backend Integration
 * Sends nodes and edges to /pipelines/parse when the button is clicked.
 * (Instructions: "update /frontend/src/submit.js")
 * Implementation is in submit.ts; this file re-exports for path compliance.
 */
export { submitPipeline } from "./submit";
